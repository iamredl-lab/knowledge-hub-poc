# Commit Style

Format: `type: short description` — `feat`, `fix`, `docs`, `chore`, `refactor`, `ci`.

The message explains _why_ the change was made, not a line-by-line recap of the diff —
the diff is already visible in `git show`. Don't list the files or functions affected
in the message — that's visible from the commit itself.
